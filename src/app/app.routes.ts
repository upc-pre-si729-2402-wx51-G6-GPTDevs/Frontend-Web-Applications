import { Routes } from '@angular/router';
import { PublishTaskComponent } from './publish-task/publish-task.component';

export const routes: Routes = [
  { path: '', component: PublishTaskComponent }  // Ruta principal
];
